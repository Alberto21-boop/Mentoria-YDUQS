Para criar o projeto em React com TypeScript do zero, siga os passos abaixo:

Abra o terminal e execute o comando para criar um novo projeto com TypeScript:
npx create-react-app todo-list --template typescript


Depois cd todo-list
npm install
npm install styled-components
npm install --save-dev @types/styled-components
